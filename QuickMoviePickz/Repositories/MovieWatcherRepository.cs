using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace QuickMoviePickz.Repositories
{
    public class MovieWatcherRepository
    {
        public MovieWatcherRepository()
        {
            // Someone logs into the app 
            // they enter into a group with whom they are watching netflix with 
            // each person fills out the survey
            // use their answers to get the api 
            // also during this step we want to average out their responses so we can return back 5 movies to them 
            //display the snynopis of the movie to them
            //each member can then vote on which one they liked the best 
            // display the movie choice and all other votes
        }
    }
}
